<?xml version="1.0" encoding="utf-8"?>
<!--
  Real values live in secrets.config (gitignored). Copy this template to
  secrets.config and fill in values before deploy. See README "Shared secret
  rotation".
-->
<appSettings>
  <add key="SharedSecret" value="REPLACE_ME" />
</appSettings>
